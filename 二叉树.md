- 前序遍历（中左右）：5 4 1 2 6 7 8
- 中序遍历（左中右）：1 4 2 5 7 6 8
- 后序遍历（左右中）：1 2 4 7 8 6 5

```js
var postorderTraversal = function (root) {
    const res = [];
    const inoder = (root) => {
        if (!root) return;
        //res.push(root.val);//前
        inoder(root.left);
        //res.push(root.val);//中
        inoder(root.right);
        res.push(root.val);//后
    }
    inoder(root);
    return res;
};
```

```js
给定一个二叉树，判断它是否是高度平衡的二叉树。

本题中，一棵高度平衡二叉树定义为：

一个二叉树每个节点 的左右两个子树的高度差的绝对值不超过 1 。
const height = (root) => {
    if (!root) {
        return 0;
    } else {
        return Math.max(height(root.left), height(root.right)) + 1;
    }
}
var isBalanced = function (root) {
    if (root == null) {
        return true;
    } else {
        return Math.abs(height(root.left) - height(root.right)) <= 1 && isBalanced(root.left) && isBalanced(root.right);//判断左右两边高度相同 && 左边子树的左右两边高度相同 && 右边子树的左右两边高度相同 递归所有的子树都是则true
    }
}










*********左右两边都没叶子的时候


//二叉树的最大高度
//二叉树最深高度
    const height = (root) => {
        if (!root) {
            return 0;
        } else {
            return Math.max(height(root.left), height(root.right)) + 1;
        }
    }

//二叉树的最小高度
    const minheight = (root) => {
        if (!root) {
            return 0;
        } else {
            if (root.left === null && root.right || root.left && root.right === null) {
                if (root.val === star) {
                    return Math.min(minheight(root.left), minheight(root.right)) + 1;
                }
                return Math.max(minheight(root.left), minheight(root.right)) + 1;
            }
            return Math.min(minheight(root.left), minheight(root.right)) + 1;
        }
    }
```

